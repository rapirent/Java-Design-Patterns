rootProject.name = "java-design-pattern"
def list = [
    '1-Strategy',
    '11-1-Remote-Proxy',
    '11-3-Protection-Proxy'
]
list.each{
    file(it).eachDir { dir ->
        include dir.name
        project(":${dir.name}").projectDir = dir
    }

}
